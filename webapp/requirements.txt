# Dependencias del portal web desplegable (Render).
# Pisos de versión verificados en local: fastapi >=0.115 es imprescindible para
# que sea compatible con starlette 1.x (las versiones antiguas usaban un
# argumento on_startup que starlette ya no acepta).
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
psycopg2-binary>=2.9
geopy>=2.4
